# Tiny API Hub — Agent 须知

先读 [docs/REQUIREMENTS.md](docs/REQUIREMENTS.md)。那是需求唯一源。

## 硬约束

- **从 0 手写。** 不要 clone、拷贝、裁剪 `../all-api-hub` 的实现文件。
- **旧仓只读参考。** 可阅读 `../all-api-hub` 对齐协议、数据形状、过盾行为，然后用新代码表达。
- **不要改旧仓。** 除用户明确要求外，不编辑 `../all-api-hub`。
- **阶段 1 只做本机可用。** 不上架、不申请商店扩展 ID。

## 实现

按 `docs/REQUIREMENTS.md` 第 5 节垂直切片。先脚手架，再 New API 账号刷新，再叠后续功能。

过盾/临时页：真实站点一拦就补，不要攒到最后。
